### 8.10. HTTP / cloud service

Generic cloud HTTPS entry points that are not owned by a more specific ABI chapter (auth, bind, presets, MakerWorld, filament, print upload). Studio resolves them via `dlsym` ([NetworkAgent.cpp:333-366](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/Utils/NetworkAgent.cpp#L333-L366)). Camera URL symbols live in [§8.11](08.11-camera.md).

Neighbors: hosts / fingerprint headers / JSON envelope ([§6.6](06.06-cloud-rest.md)); auth ([§8.5](08.05-auth.md)); bind ([§8.6](08.06-bind.md)); presets ([§8.9](08.09-presets.md)); MakerWorld ([§8.12](08.12-makerworld.md)); filament ([§8.15](08.15-filament.md)); cloud print REST ([§8.8.3](08.08-print-abi.md#883-bambu_network_start_local_print_with_record) / [§8.8.4](08.08-print-abi.md#884-bambu_network_start_print)).


| Symbol | Signature | Notes |
| --- | --- | --- |
| `bambu_network_set_extra_http_header` | `int(void*, std::map<std::string, std::string>)` | Startup fingerprint map |
| `bambu_network_get_studio_info_url` | `std::string(void*)` | No HTTP; string accessor |
| `bambu_network_get_user_print_info` | `int(void*, unsigned int* http_code, std::string* http_body)` | Device list / access codes |
| `bambu_network_get_user_tasks` | `int(void*, TaskQueryParams, std::string* http_body)` | Print History |
| `bambu_network_get_subtask_info` | `int(void*, std::string subtask_id, std::string* task_json, unsigned int* http_code, std::string* http_body)` | Device-panel cover / plate |
| `bambu_network_get_task_plate_index` | `int(void*, std::string task_id, int* plate_index)` | Legacy; no GUI on current pin |
| `bambu_network_get_slice_info` | `int(void*, std::string project_id, std::string profile_id, int plate_index, std::string* slice_json)` | Legacy; no GUI on current pin |
| `bambu_network_get_my_message` | `int(void*, int type, int after, int limit, unsigned int* http_code, std::string* http_body)` | No GUI on current pin |
| `bambu_network_check_user_task_report` | `int(void*, int* task_id, bool* printable)` | No GUI on current pin |
| `bambu_network_report_consent` | `int(void*, std::string expand)` | Privacy / improvement consent |
| `bambu_network_post_device_region` | `int(void*, DeviceRegionParams, std::string* http_body)` | Startup region query; since `02.08.04` |


#### 8.10.1. Common contract

**`TaskQueryParams`** ([bambu_networking.hpp:252-258](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/Utils/bambu_networking.hpp#L252-L258)) — by-value argument of `get_user_tasks`:

| Field | Type | Default | Wire (`GET …/my/tasks`) | Meaning |
| --- | --- | --- | --- | --- |
| `dev_id` | `std::string` | — | `deviceId` when non-empty | Filter tasks for one printer; omit query key when empty |
| `status` | `int` | `0` | `status` | Cloud task-status filter (Studio / WebView pass-through; `0` = unrestricted / “all” in observed callers) |
| `offset` | `int` | `0` | `offset` | Pagination offset |
| `limit` | `int` | `20` | `limit` | Page size |

**Shared transport.** Paths below are relative to the regional API host from [§6.6 Cloud REST API](06.06-cloud-rest.md). Auth is the usual `Authorization: Bearer <access_token>` unless noted. Many symbols write raw HTTP status into `unsigned int* http_code` and/or the response body into `std::string* http_body` / `task_json` / `slice_json` — Studio parses JSON itself.

**Evidence tags** (used on each symbol):

| Tag | Meaning |
| --- | --- |
| `MITM` | Seen in a live dump of the stock plugin |
| `probe` | Issued by hand (`curl` / `plugin_runner --action http_probe`) against production |
| `source` | Read out of Studio’s own code |
| `stub` | Plugin never hits the network; Studio accepts a canned response |

**Return convention.** Most symbols return `int`: `0` on success, non-zero on failure. `get_studio_info_url` returns a URL string. There is no dedicated `BAMBU_NETWORK_ERR_*_HTTP_*` family for this surface in [§8.16](08.16-errors.md) beyond generic codes where Studio cares.


#### 8.10.2. `bambu_network_set_extra_http_header`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `extra_headers` | `std::map<std::string, std::string>` | Fingerprint / region headers Studio wants on subsequent HTTPS. |

**Purpose.** Store extra HTTP headers the plugin must attach to later cloud requests.

**Studio.** [`GUI_App::init_http_extra_header`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L2678-L2691) during cold boot and again on region/language switches ([§8.1](08.01-initialization.md)). Wrapper: [NetworkAgent.cpp:1283-1291](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/Utils/NetworkAgent.cpp#L1283-L1291).

**Plugin behaviour.** Pure state update — no HTTP. Keep the map and fold it into outgoing header sets ([§6.6](06.06-cloud-rest.md)). The server ignores most of these headers (analytics / fingerprint). Evidence: source.

**Return.** `0` on success; non-zero on failure.

#### 8.10.3. `bambu_network_get_studio_info_url`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |

**Purpose.** Return the base Studio “slicer resource” URL string (no network I/O).

**Studio.** Early `GUI_App` / `PresetUpdater` appended query params for news/banner / updater. **[BambuStudio `ba049f6a2`](https://github.com/bambulab/BambuStudio/commit/ba049f6a2e08c3b6033660bb84da80c08722974b) has no GUI call sites** (symbol still `dlsym`'d).

**Plugin behaviour.** String accessor, **no HTTP**. Stock `02.08.01.53` returns `https://api.bambulab.com/v1/iot-service/api/slicer/resource` (CN host when region is CN). Evidence: `plugin_runner --action http_probe` + source (historical).

**Return.** URL string (may be empty if unimplemented).

#### 8.10.4. `bambu_network_get_user_print_info`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `http_code` | `unsigned int*` | out: HTTP status. |
| `http_body` | `std::string*` | out: raw JSON body for Studio’s device-list parser. |

**Purpose.** Fetch the signed-in user’s cloud printer inventory (names, online flags, plaintext `dev_access_code`, …).

**Studio.** [`DeviceManager`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/DeviceCore/DevManager.cpp#L878) (and related refresh paths) via [NetworkAgent.cpp:1315-1322](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/Utils/NetworkAgent.cpp#L1315-L1322).

**Plugin behaviour.** Two confirmed endpoints serve this data (stock has been observed on both):

1. `GET /v1/iot-service/api/user/bind` — `{ "devices":[{ "dev_id", "name", "online", "print_status", "dev_model_name", "dev_product_name", "dev_access_code", "print_job", "nozzle_diameter", "dev_structure", … }]}`. Uses `name` / `online` / `print_status` — requires remapping to Studio’s expected `dev_name` / `dev_online` / `task_status`. Evidence: MITM + probe.

2. `GET /v1/iot-service/api/user/print?force=true` — `{ "devices":[{ "dev_id", "dev_name", "dev_model_name", "dev_product_name", "dev_online", "dev_access_code" }]}`. Field names match Studio’s parser directly. Also polled as a cloud print-job queue check (empty queue → `{"message":"success","code":0,"error":null}` with no `devices` key). Evidence: SSLKEYLOGFILE capture.

**Security note:** `dev_access_code` is returned in plaintext by both endpoints — see [§10.6](10.06-lan-tls-and-access-codes.md). Write status / body into the out-params.

**Return.** `0` on success; non-zero on failure.

#### 8.10.5. `bambu_network_get_user_tasks`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `params` | `TaskQueryParams` | Filters / pagination ([§8.10.1](#8101-common-contract)). |
| `http_body` | `std::string*` | out: raw JSON body. |

**Purpose.** List cloud print tasks for the Task / History UI.

**Studio.** [`TaskManager`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/TaskManager.cpp#L329) and Print History WebView ([WebViewDialog.cpp:1392](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/WebViewDialog.cpp#L1392)). Wrapper: [NetworkAgent.cpp:1325-1332](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/Utils/NetworkAgent.cpp#L1325-L1332).

**Plugin behaviour.** `GET /v1/user-service/my/tasks?limit=&offset=&status=[&deviceId=]` with Bearer auth (`dev_id` → `deviceId` when non-empty). Pass the response through unchanged:

```json
{ "total": N, "hits":[{ "id", "title", "deviceId", "deviceName", "cover", "status", "startTime", "endTime", "profileId", "designId", "designTitle", "mode", "..." }] }
```

Evidence: MITM of stock `bambu_network_agent/02.08.01.51`.

**Return.** `0` on success; non-zero on failure.

#### 8.10.6. `bambu_network_get_subtask_info`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `subtask_id` | `std::string` | Cloud subtask / task id. |
| `task_json` | `std::string*` | out: parsed/selected JSON Studio uses for the card (stock may mirror `http_body`). |
| `http_code` | `unsigned int*` | out: HTTP status. |
| `http_body` | `std::string*` | out: raw response body. |

**Purpose.** Fetch device-panel cover / plate metadata for an active (or recent) cloud print.

**Studio.** [`MachineObject::update_slice_info`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/DeviceManager.cpp#L3886-L3923). Wrapper: [NetworkAgent.cpp:1438-1442](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/Utils/NetworkAgent.cpp#L1438-L1442). Studio pulls the printer-card hero from `context.plates[<plate_idx>].thumbnail.url` and downloads the PNG via wxWebRequest; `content` is a JSON *string* holding `{info:{plate_idx}}`.

**Plugin behaviour.** `GET /v1/iot-service/api/user/task/<subtask_id>` (Bearer; stock `User-Agent: bambu_network_agent/…`). Forward the body. `plates[]` entries also carry `prediction`, `weight`, `filaments[]` (what legacy `get_slice_info` used to supply). Evidence: MITM stock `02.08.01.53` (Device tab + `http_probe`) + Studio source.

**Return.** `0` on success; non-zero on failure.

#### 8.10.7. `bambu_network_get_task_plate_index`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `task_id` | `std::string` | Cloud task / subtask id. |
| `plate_index` | `int*` | out: plate index. |

**Purpose.** Look up which plate a cloud task ran on.

**Studio.** Legacy: early `DeviceManager::update_slice_info` when `plate_idx < 0`. **[BambuStudio `ba049f6a2`](https://github.com/bambulab/BambuStudio/commit/ba049f6a2e08c3b6033660bb84da80c08722974b):** removed; plate comes from `push_status` / `get_subtask_info` `content.info.plate_idx`. Wrapper still present: [NetworkAgent.cpp:1405-1413](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/Utils/NetworkAgent.cpp#L1405-L1413).

**Plugin behaviour.** Same URL as `get_subtask_info`: `GET /v1/iot-service/api/user/task/<id>`; set `*plate_index` from `content.info.plate_idx` (probe: id `1114566547` → plate `1`). Evidence: MITM via `http_probe` + source (historical).

**Return.** `0` on success; non-zero on failure.

#### 8.10.8. `bambu_network_get_slice_info`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `project_id` | `std::string` | Cloud project id. |
| `profile_id` | `std::string` | Cloud profile id. |
| `plate_index` | `int` | Plate index (Studio arg; **not** placed in the stock URL). |
| `slice_json` | `std::string*` | out: slice summary JSON. |

**Purpose.** Fetch slice summary for a cloud project/profile/plate.

**Studio.** Legacy: early `update_slice_info` after `get_task_plate_index`; replaced by **`get_subtask_info`** (`98a7a10ce` / `16cee3299`). **No GUI call site through [BambuStudio `ba049f6a2`](https://github.com/bambulab/BambuStudio/commit/ba049f6a2e08c3b6033660bb84da80c08722974b).**

**Plugin behaviour.** Stock: `GET /v1/iot-service/api/user/project/<project_id>?profile_id=<profile_id>` (Bearer; `plate_index` is **not** in the URL). Empty `project_id` → `…/project/?profile_id=…` **405**; `model_id` as project → **422**; wrong / foreign id → **403/400**. On success the HTTP body is the full project envelope (`project_id`, `model_id`, `profiles[]`, …); stock then picks `profiles[*].context.plates[]` with `index == plate_index` and writes that **plate object** into `*slice_json` as pretty-printed JSON (`indent=4`, keys sorted alphabetically). Example plate fields (stock `02.08.01.53` via `http_probe`, project from a prior cloud print):

```json
{
    "filaments": [
        {
            "color": "#E06640",
            "filament_id": "Pd4a7530",
            "id": "1",
            "type": "PLA",
            "used_for_object": true,
            "used_for_support": false,
            "used_g": "18.64",
            "used_m": "6.25"
        }
    ],
    "gcode": { "bucket": "", "dir": "", "name": "", "url": "" },
    "index": 1,
    "prediction": 2160,
    "thumbnail": { "bucket": "or-cloud-model-prod", "dir": "Metadata", "name": "plate_1.png", "url": "https://…/plate_1.png?…" },
    "weight": 18.64
}
```

(Other plate keys also present: `top_picture` / `pick_picture` / `no_light_picture`, `objects`, `warning`, `layer_filament_lists`, ….) Legacy Studio parsers only needed `prediction` / `weight` / `thumbnail` / `filaments[]` (`color`, `type`, `used_g`, `used_m`). Print-history / `get_subtask_info` tasks often have empty `project_id` — those cannot drive this call; a real id comes from the cloud print pipeline (`POST …/user/project`). Evidence: MITM `stock_http_probe_slice.mitm` + `http_probe` (success) / earlier error probes + source (historical).

**Return.** `0` on success; non-zero on failure.

#### 8.10.9. `bambu_network_get_my_message`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `type` | `int` | Message type filter. |
| `after` | `int` | Pagination cursor. |
| `limit` | `int` | Page size. |
| `http_code` | `unsigned int*` | out: HTTP status. |
| `http_body` | `std::string*` | out: raw body. |

**Purpose.** Message Centre inbox poll.

**Studio.** ABI added `497be311d`; **no GUI call site through [BambuStudio `ba049f6a2`](https://github.com/bambulab/BambuStudio/commit/ba049f6a2e08c3b6033660bb84da80c08722974b)** (NetworkAgent wrappers only — [NetworkAgent.cpp:1294-1302](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/Utils/NetworkAgent.cpp#L1294-L1302)).

**Plugin behaviour.** Stock `02.08.01.53`: `GET /v1/user-service/my/messages/type=<t>&after=<a>&limit=<n>` (path uses `/` before the param list — **not** `?type=…`). Production answered **404** `"404 page not found"` for `type=0` (`rc!=0`, `http_code=404`). Evidence: MITM via `http_probe`.

**Return.** `0` on success; non-zero on failure.

#### 8.10.10. `bambu_network_check_user_task_report`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `task_id` | `int*` | out: task id to rate, or sentinel when nothing pending. |
| `printable` | `bool*` | out: whether a rate prompt is appropriate. |

**Purpose.** Decide whether to show a “rate this print” prompt.

**Studio.** NetworkAgent wrappers only — **no GUI call site through [BambuStudio `ba049f6a2`](https://github.com/bambulab/BambuStudio/commit/ba049f6a2e08c3b6033660bb84da80c08722974b)** ([NetworkAgent.cpp:1305-1312](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/Utils/NetworkAgent.cpp#L1305-L1312)).

**Plugin behaviour.** Stock probe (`http_probe`, logged-in, nothing pending — reconfirmed with a fresh session): **no dedicated HTTPS** between the call and return (MITM shows only the surrounding `get_my_message` / `get_task_plate_index` / `get_slice_info` URLs); outs were `task_id=-1`, `printable=true`, `rc=0`. No Studio GUI caller remains to force a non-empty report queue; endpoint for that case is still unconfirmed. Evidence: MITM via `http_probe` (`stock_http_probe_slice.mitm`).

**TODO**: need capture of stock `check_user_task_report` when a rateable print exists (no live GUI path; idle path is local-only).

**Return.** `0` on success; non-zero on failure.

#### 8.10.11. `bambu_network_report_consent`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `expand` | `std::string` | Misnamed: **entire JSON body** to POST (not an expand flag). |

**Purpose.** Report privacy / improvement / Helio consent choices to the cloud when the user is logged in.

**Studio.** [`GUI_App::report_consent_common`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L8228-L8256) (startup / privacy history / Privacy Policy Update), `HelioStatementDialog::report_consent_install`, `ExpandCenterDialog::report_consent_unstall`. If logged in → `NetworkAgent::report_consent(body)` ([NetworkAgent.cpp:1084-1088](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/Utils/NetworkAgent.cpp#L1084-L1088)); else Studio itself `POST`s the same body via [`GUI_App::report_consent`](https://github.com/bambulab/BambuStudio/blob/12f17b06f4f537f9c03162d08bb70cf733c42839/src/slic3r/GUI/GUI_App.cpp#L5937). Studio ignores the return value; local `app_config` already records the choice.

**Plugin behaviour.** When logged in, stock posts the ABI `expand` string as the HTTPS body:

`POST /v1/user-service/user/consent` — `User-Agent: bambu_network_agent/<ver>`, Bearer auth, e.g.

```json
{"consentBody":"{\"formList\":[{\"formID\":\"StudioImprovementPolicy\",\"op\":\"Opt-in\"}],\"scene\":\"studio_improvement_policy_enable\",\"version\":1}"}
```

HTTP **200** with empty body on success. Same URL / body shape as Studio’s logged-out `GUI_App::report_consent` path (that path uses `User-Agent: BambuStudio/…` instead). `op` is `Opt-in` / `Withdraw`; CN appends `-CN` to `formID`. Scenes include `studio_improvement_policy_enable`, `studio_privacy_policy_enable`, `helio_enable`. Evidence: MITM stock logged-in (`stock_history.mitm`, `stock_hybrid.mitm`, UA `bambu_network_agent/02.08.01.53`) + Studio source.

**Return.** `0` on success; non-zero on failure (Studio ignores).

#### 8.10.12. `bambu_network_post_device_region`

**Parameters.**

| Name | Type | Meaning |
| --- | --- | --- |
| `agent` | `void*` | Opaque agent from `create_agent`. |
| `params` | `DeviceRegionParams` | See below. Studio fills only `ClientType`. |
| `http_body` | `std::string*` | out: raw HTTP response body, including on non-2xx. |

**`DeviceRegionParams`** (ABI ≥ `02.08.04`, field order matches upstream):

| Field | Type | What stock does with it |
| --- | --- | --- |
| `DeviceId` | `std::string` | Not placed on the request. Studio leaves it empty; the device id the server wants is the separate `X-BBL-Device-ID` extra header (Studio's `slicer_uuid`). |
| `ClientType` | `std::string` | Sole JSON key. Empty becomes the literal `slicer`, which is not copied from the `X-BBL-Client-Type` header. |
| `country` | `std::string` | Not placed on the request. |
| `XClientCountry` | `std::string` | Not placed on the request. Confirmed with `FR` as well as values containing a space, `+` and `&`. |

**Purpose.** One-shot startup query. Studio logs the body and does not branch on it.

**Studio.** [`GUI_App::post_device_region`](https://github.com/bambulab/BambuStudio/blob/v02.08.04.57/src/slic3r/GUI/GUI_App.cpp) runs on the `check_cert` worker, once per launch. It sets `ClientType = "slicer"` and nothing else. `X-BBL-Device-ID` is installed earlier by [`GUI_App::get_extra_header`](https://github.com/bambulab/BambuStudio/blob/v02.08.04.57/src/slic3r/GUI/GUI_App.cpp) from `app_config` `slicer_uuid`, via `set_extra_http_header`.

**Plugin behaviour.** `POST /v1/user-service/device/region` on the regional API host (`api.bambulab.com`, or `api.bambulab.cn` when `set_country_code` is `CN`). Evidence: MITM (`02.08.04.60`).

```json
{"ClientType":"slicer"}
```

`Authorization: Bearer …` is attached only when a session exists; a logged-out agent still posts. `Content-Type: application/json` stays. Extra headers from `set_extra_http_header` go out too, which is how `X-BBL-Device-ID` arrives. No local validation of the four fields.

Without that device-id header the server answers **400** `field "x-bbl-device-id" is not set` and stock returns `−60`, body forwarded. With the header set, the same call returned **200**:

```json
{"vdc":"us","storeRegion":"RS"}
```

`block_cloud = 1` (the default) does not call the network: return `0` and an empty body.

**Return.** `0` on HTTP 2xx. Transport error or non-2xx → `BAMBU_NETWORK_ERR_POST_DEVICE_REGION_FAILED` (−60) ([§8.16](08.16-errors.md)). A null agent, when `block_cloud` is off, returns `BAMBU_NETWORK_ERR_INVALID_HANDLE`.

Hub: [§8](08-abi-main.md).
